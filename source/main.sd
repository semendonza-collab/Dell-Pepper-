module mainapp;
import std.stdio;
import  std.array;

public void main()
{
 int number_of_notes; 

 bool listing = 0;                                        // everything here is important dont get rid of anything or i will chop my penis off and shove it up youre ass
 bool isListing;             

 string UserNote; 
 readfln(UserNote);

 int[] list = ["Note", "Note", "Note", "Note"];

if(UserNote == "list" || "List")
 {
   listing = 1; 

   if(listing == 1)
   {
    isListing = true;
   }
   else {
     isListing = false;
   }

 }
 if(listing)
 {
   for(double i = 0; 1 < number_of_notes; i++)
 {
  writeln(list[1]);
 }
} 
 else 
 {
   writeln("Please use the !Help command.");
 }
  
}



